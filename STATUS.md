# Release status

Current status: **Candidate** — E2E adaptation tutorial source complete, awaiting supported clean-runtime execution. The pipeline package, 36 offline unit tests, one executed local CPU run of the committed notebook blob, `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/esm2_protein_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Unit tests and static conformance checks run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence from a supported hosted runtime for the exact candidate commit is recorded according to `docs/release-verification.md`.
